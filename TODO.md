# TODO: Add Confusion Matrix and Classification Report Printing

## Tasks
- [x] Update `src/preprocess.py` to use `DATA_PATH` from config instead of hardcoding the path
- [x] Remove duplicated chatbot code from the end of `src/train.py`
- [x] Update imports in `src/serve_streamlit.py` to use `from src.config import ...` for consistency
- [x] Test the changes to ensure functionality is preserved
- [x] Add confusion matrix and classification report printing for each model in `src/train.py`
